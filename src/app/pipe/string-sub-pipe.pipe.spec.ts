import { StringSubPipePipe } from './string-sub-pipe.pipe';

describe('StringSubPipePipe', () => {
  it('create an instance', () => {
    const pipe = new StringSubPipePipe();
    expect(pipe).toBeTruthy();
  });
});
